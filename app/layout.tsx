import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Chiliz Sports | When Access Matters, We\'re Already Inside',
  description: 'The sports partnership insiders. Access to rights & talent from global brands to clubs, athletes, and influencers.',
  keywords: 'sports marketing, sponsorship, brand partnerships, athlete ambassadors, Chiliz',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
